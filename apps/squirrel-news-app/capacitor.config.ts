import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'squirrel-news-app',
  webDir: '../../dist/apps/squirrel-news-app',
  bundledWebRuntime: false,
};

export default config;
